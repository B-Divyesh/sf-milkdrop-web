{
  "name": "milkdrop-web",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/generate-sw.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:browser": "playwright test",
    "test:claims": "playwright test tests/claims.spec.ts",
    "test:watch": "vitest",
    "audit": "node scripts/browser-audit.mjs",
    "audit:pwa": "node scripts/pwa-audit.mjs"
  },
  "dependencies": {
    "peerjs": "^1.5.5",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.58.2",
    "@types/qrcode": "^1.5.5",
    "playwright": "^1.58.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
